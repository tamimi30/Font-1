// build.gradle (Project-level)
buildscript {
    repositories {
        mavenCentral()
        google()
        // أضف مستودع Chaquopy
        maven { url "https://chaquo.com/maven" }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.2'
        // أضف إضافة Chaquopy Gradle plugin
        classpath "com.chaquo.python:gradle:12.1.0"
    }
}

allprojects {
    repositories {
        mavenCentral()
        google()
        // أضف مستودع Chaquopy هنا أيضاً في حال احتاجه أحد الموديولات
        maven { url "https://chaquo.com/maven" }
    }
}